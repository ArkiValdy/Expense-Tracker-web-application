{
  "name": "expense-tracker",
  "version": "2.0.0",
  "private": true,
  "description": "Full-stack Expense Tracker SPA (React + Express + MongoDB) with JWT auth, live search, and an admin panel.",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "export": "npm run export --prefix backend",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
